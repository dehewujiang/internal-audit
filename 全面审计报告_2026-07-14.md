# 内部审计AI系统 — 全面审计报告

> 审计日期：2026-07-14  
> 审计对象：`D:\Nut\00_my_digital\12_AGI\skills\internal-audit\`  
> 审计方法：静态代码审查 + 文档一致性校验 + 审计实务对标（未运行动态测试）  
> 审计基准：constitution.md 11条硬约束 + 通用软件工程良好实践 + 中国内部审计三十年实务经验

---

## 审计结论速览

这套系统在**设计层面**展现了相当高的专业水准——地铁闸机模型、三层防御体系、证据等级强制校验，都是同类AI审计工具里少见的硬骨头。但它在**工程落地层面**存在若干"AI帮一家公司做了专用工具"的痕迹：硬编码路径、硬编码公司特征、零测试覆盖。系统目前像一把为某个审计经理量身打的猎枪，精度高但搬不动。

| 维度 | 高风险 | 中风险 | 低风险 |
|------|:------:|:------:|:------:|
| 业务角度 | 2 | 3 | 2 |
| 系统工程 | 2 | 5 | 1 |
| **合计** | **4** | **8** | **3** |

---

## 维度一：业务角度（内部审计专家视角）

### 设计亮点（先说做得对的）

1. **证据等级强制校验落地**——constitution第3条"高风险finding必须有A级（系统导出）或E级（第三方）证据"，不是空头支票。`validate-finding.py` 第334-337行真的检查、第449行标记blocker、第675-679行`--strict`下`sys.exit(1)`阻断写入。这是很多审计系统做不到的——嘴上说要有证据，代码里不拦。这里拦了。

2. **"证据缺失即信号"（第9条）的实务洞察**——把"没拿到证据"本身当线索，要求分析三种可能（业务未发生/管理缺失/证据被消除）。这符合舞弊调查思维：拿不到证据 sometimes 就是证据。

3. **"不得法律定性"（第6条）守住边界**——只能说"疑似舞弊"不能说"构成舞弊"，这是中国内审的正确分寸，审计不是法院。

4. **阶段闸机防跳步**——6阶段流转由确定性代码管，AI不能自己跳闸机。符合审计底稿完整性要求。

5. **证据链不可断裂（第11条）**——步骤ID一旦使用永久保留，纠错只能追加`-C`后缀步骤。符合审计工作底稿"不得涂改"的规范。

### 问题与风险

#### B1. 企业真实敏感数据未保护 🔴 高

**问题**：`audit-topics/about-me.md` 含真实企业经营数据——年销售收入21亿、客户名单（一汽大众/上汽大众/比亚迪等十余家）、四家子公司全名（宁波长盛/宁波长华/武汉长源/广东长华）、员工2000人。`.gitignore` 未排除该文件。

**风险**：一旦代码库托管到Git（含内部GitLab），这些数据即进入版本历史，即便后续删除也留有记录。客户名单和子公司信息属企业商业秘密，泄露可能违反保密协议。

**佐证**：`about-me.md` 第4行；`.gitignore` 未含 `audit-topics/about-me.md`。

**改进建议**（立即修）：
- `.gitignore` 增加排除 `audit-topics/about-me.md` 和 `audit-topics/my-config.md`
- 或将真实企业数据移到项目部署目录（`internal-audit-workspace/`），源仓库只留模板
- 若已提交过Git，需用 `git filter-branch` 或 BFG 清除历史记录

#### B2. 共享校验脚本绑死单一客户 🔴 高

**问题**：`validate-program.py` 第69-82行 `check_company_facts()` 硬编码了"21亿/2000人/紧固件/冲焊/SAP/MES/泛微/62.24%/FLAN/宁波/武汉/广东/大众/丰田/本田/比亚迪"作为"公司具体事实"校验依据。要求程序文本至少命中2条才算合格。

**风险**：这是**通用校验工具**（位于`_shared/scripts/`，所有项目共用），却绑定了单一客户特征。换家公司审计时——比如审计一家北京的软件公司——该校验会误报"引用公司事实不足"，要么误导审计师补充无关内容，要么被绕过失去校验意义。违反审计工具的**独立性原则**。

**佐证**：`validate-program.py` 第72-78行 `company_markers` 列表。

**改进建议**（立即修）：
- 校验逻辑改为：从该项目的 `about-me.md` 动态提取公司特征（营收数字、员工数、主营产品、地名、客户名），再检查程序是否引用
- 或降级为"建议项"而非"校验项"——提示"建议引用公司具体事实"，但不阻断

#### B3. "10条硬约束"标题与实际11条矛盾 🟡 中

**问题**：constitution.md 实际有11条硬约束（编号1-11），但 `CLAUDE.md` 第250行标题写"10 hard constraints"、`CLAUDE-project.md` 同样、`OPS.md` 第189行写"这10条规则"。三处文档都少算一条。

**风险**：审计师看 `OPS.md` 以为只有10条底线，漏掉第11条"证据链不可断裂"的认知。AI 读 `CLAUDE.md` 也可能只关注前10条。文档准确性是审计系统的基本要求——连自己的规则都数错，说服力打折。

**佐证**：constitution.md 第36行（第11条）；CLAUDE.md 第250行；OPS.md 第189行。

**改进建议**（近期排期）：三处文档统一改为"11条硬约束"。建议用一个变量或脚本生成该数字，避免手动维护。

#### B4. 舞弊调查专项保护不足 🟡 中

**问题**：`about-me.md` 明确"舞弊调查是目前的重点工作领域"。但系统对舞弊场景的专项控制薄弱——`whistleblower_pending`（举报待处理）标志在 `phase_gate.py` 仅触发 `prompt_update` 提示（第164-165行），没有：举报人信息隔离、舞弊证据链加密访问、舞弊finding的额外审批层。

**风险**：舞弊调查的敏感度远高于常规审计。举报人信息若在文件系统中明文存放（JSON/MD），可能被不当访问。舞弊线索若只在 `prompt_update` 软提示层，审计师可能 `--force` 跳过而无强制记录。

**佐证**：`phase_gate.py` 第164-165行；`about-me.md` 第1行（舞弊调查重点）。

**改进建议**（近期排期）：
- 举报材料单独目录 + 访问日志
- `whistleblower_pending` 为 true 时，`--force` 跳过需额外记录理由到 `audit_trail`（目前force只记"强制调用"，不区分舞弊场景）

#### B5. 跨项目查询能力未经实战验证 🟡 中

**问题**：`audit-topics/projects-index.json` 是空数组 `{"projects": []}`；`data/evaluations/` 仅1条记录（2026-05-12.jsonl，3114字节）。`OPS.md` 和 `CLAUDE.md` 大力宣传的"跨项目查询/趋势分析/年度对比"能力，从未真正跑通过。

**风险**：未经验证的功能写入操作手册，审计师可能依赖一个实际不work的能力做决策。比如"对比2025和2026存货审计"——如果跨项目查询有bug，对比结果错误，审计师基于错误对比下结论。

**佐证**：`projects-index.json` 内容为空；`data/evaluations/` 仅1文件。

**改进建议**（近期排期）：用至少2个真实（或模拟）审计项目跑通注册→查询→对比全链路，确认无误后再在 `OPS.md` 宣传。

#### B6. 缺真实审计workspace端到端验证 🟢 低

**问题**：源仓库无 `internal-audit-workspace/`（部署后才生成），所有评估基于设计层面，未验证"真跑一个完整审计时是否按设计执行"。

**风险**：设计与执行之间可能有gap——比如某SKILL.md写"Step 3调用validate"，实际AI可能漏调。这是本次审计的已知盲区。

**改进建议**（长期演进）：用一个完整审计主题从Phase 0走到Phase 5，记录每个闸机实际行为，对照设计文档找偏差。

#### B7. OPS.md缺"何时不该用AI"的边界 🟢 低

**问题**：操作手册告诉审计师"系统帮你做什么"，但没说"什么不该交给AI"。比如重大舞弊定性、涉及高管的结论，仍应人工判断。

**改进建议**（长期演进）：OPS.md增补"AI边界"章节，列明哪些判断必须人工把关。

---

## 维度二：系统工程角度

### 设计亮点

1. **地铁闸机模型（phase_gate.py）设计优秀**——阶段转换由确定性代码（553行）管理，不依赖LLM记忆。AI不能跳闸机、绕闸机、搬闸机。这是AI Native系统的正确架构：把"必须确定的"交给代码，把"可以判断的"交给AI。

2. **三层防御体系清晰**——流程闸机（phase_gate check/advance）+ 质量闸机（validate --strict）+ 授权闸机（tool-check）。各司其职，exit code区分pass/block/prompt。

3. **文件系统传递状态**——无运行时数据库依赖，状态全在JSON/MD文件。可审计性强，人工可读，故障可恢复。

4. **L3头部注释规范**——多数脚本有 INPUT/OUTPUT/POS 三段头，符合内部规则。

5. **junction/stable双部署模式**——开发用junction实时同步，生产用stable锁定版本。设计合理。

### 问题与风险

#### S1. 硬编码绝对路径 🔴 高

**问题**：`internal-audit-evaluator/quality_gate.py` 第34行硬编码 `D:/Nut/00_my_digital/12_AGI/skills/internal-audit/audit-topics/my-config.md`，第44行硬编码 `.../data/evaluations`。

**风险**：部署到其他机器（路径不同）必失效。`os.path.expanduser()` 不会展开这种已是绝对路径的字符串。质量闸机静默失效——找不到配置就返回默认路径，校验可能基于错误阈值通过。

**佐证**：`quality_gate.py` 第31-44行。

**改进建议**（立即修）：路径改为相对推导——从脚本自身位置 `Path(__file__).resolve()` 向上找 `audit-topics/` 和 `data/`，或读环境变量 `INTERNAL_AUDIT_SKILLS_DIR`（phase_gate.py已有此机制，复用）。

#### S2. 零测试覆盖 🔴 高

**问题**：27个Python文件，无任何 `test_*.py` 或 `*_test.py`。`validate-finding.py` 的blocker判定逻辑（高风险证据校验）、`phase_gate.py` 的阶段流转逻辑——这些是系统命脉，却无回归保护。

**风险**：任何一次修改都可能悄悄破坏校验逻辑而不自知。比如改了 `check_evidence_grade()` 的某行，高风险finding可能不再被阻断——而没人知道，因为没测试会报红。对于一套管审计结论的系统，这是不可接受的风险。

**改进建议**（立即修，分批）：优先为 `validate-finding.py`（blocker逻辑）和 `phase_gate.py`（阶段流转）写单元测试。构造"高风险但缺A级证据"的测试用例，断言 `--strict` 下 `sys.exit(1)`。

#### S3. 异常静默吞没 🟡 中

**问题**：
- `phase_gate.py` 第161行 `except Exception: pass`——读取设计观察JSON失败时静默跳过
- `query_commands.py` 多处 `except json.JSONDecodeError: continue`
- `query_data_sources.py` `except Exception: continue`

**风险**：违背constitution第10条"错误容忍"原则——原文要求"工具调用失败时记录到uncertainties"。静默 `pass`/`continue` 会让证据文件损坏、JSON格式错误等问题无声消失，审计师不知道有数据没读到。在审计场景，"没读到的数据"可能恰是关键证据。

**佐证**：`phase_gate.py` 第155-162行。

**改进建议**（近期排期）：所有 `except` 至少 `logging.warning()` 或写入 `uncertainties` 列表，不得裸 `pass`。

#### S4. CLAUDE.md与CLAUDE-project.md不同步 🟡 中

**问题**：`CLAUDE.md`（源仓库用）比 `CLAUDE-project.md`（部署到审计项目用）多出：部署架构说明、L3规则、Rules loading junction trick、Workflow discipline 第7-8条（任务完成闸机、VERSION.json管理）。

**风险**：AI在审计项目里读的是精简版 `CLAUDE-project.md`，看不到"任务完成闸机"和"VERSION.json必须随commit更新"等纪律。源仓库的约束没传递到执行现场。

**佐证**：两文件diff，CLAUDE.md 第54-76行（部署架构）、第179-188行（workflow 7-8）在CLAUDE-project.md缺失。

**改进建议**（近期排期）：CLAUDE-project.md补齐缺失章节，或改为"include"机制避免双份维护。

#### S5. 版本管理缺陷 🟡 中

**问题**：
- `bump-version.py` 的 `--add` 不查重，`VERSION.json` 出现3条完全相同的"新增版本自动管理脚本"记录（第33-45行）
- `.gitignore` 未排除 `.workbuddy/`、`.omo/`、`.codegraph/`、`.agents/`、`memory/forks/` 等开发元数据目录

**风险**：版本日志失真（重复条目污染变更历史）；开发元数据可能含临时笔记/草稿，误提交泄露内部信息。

**佐证**：`VERSION.json` 第33-45行；`.gitignore` 内容。

**改进建议**（近期排期）：`bump-version.py` 增加去重（比对最近一条的file+summary）；`.gitignore` 补充排除开发元数据目录。

#### S6. 代码异味 🟡 中

**问题**：
- `tools/pdf_ocr_extractor.py` 模块级 `print` 语句（import时即输出）
- `internal-audit-report-generator/report_generator.py` 第58行裸 `except:`
- `.agents/skills/` 与 `.claude/skills/` 各有 `geb-bootstrap`/`geb-workflow` 的SKILL.md，内容不同（漂移风险）

**风险**：模块级print污染所有import该模块的脚本输出；裸except吞掉所有异常含KeyboardInterrupt；两份GEB技能副本未来维护会越来越不一致。

**改进建议**（近期排期）：print移入 `if __name__=="__main__"`；裸except改 `except Exception`；GEB技能确定唯一源，另一份改junction或删除。

#### S7. 配置字段名不一致 🟡 中

**问题**：`program_templates.json` 实际字段名是 `columns`，但 `internal-audit-program-generator/SKILL.md` 引用为 `markdown_columns`。

**风险**：AI按SKILL.md指引读取 `markdown_columns` 会取到None，回退或报错。

**改进建议**（近期排期）：统一字段名，或在SKILL.md注明"字段名为columns"。

#### S8. 目录与文件组织 🟢 低

**问题**：`tools/` 混放代码（pdf_ocr_extractor.py）与文档（13个.md）；`参考文件/AI Aagent标准.md` 拼写错误（"Aagent"）且未被任何文件引用。

**改进建议**（长期演进）：`tools/` 下的.py移入 `_shared/scripts/`；修正拼写；确认孤立文件是否保留。

---

## 交叉风险（业务×技术交汇点）

| 交汇点 | 业务问题 | 技术表征 | 综合风险 |
|--------|----------|----------|----------|
| 工具未解耦 | B2 校验绑死单一客户 | S1 路径硬编码 | 同一根因：工具与具体环境耦合，换公司/换机器即失效 |
| 敏感数据治理 | B1 企业数据未保护 | S5 gitignore不全 | 敏感数据可能随git提交进入版本历史 |
| 舞弊场景健壮性 | B4 舞弊保护薄弱 | S3 异常静默吞没 | 舞弊线索可能在异常中被丢弃且无记录 |
| 质量保证可信度 | B6 缺端到端验证 | S2 零测试 | 既无自动化测试又无实战验证，质量全靠设计审查 |

---

## 优先级行动清单

### 立即修（1周内）
1. **B1+S5**：`.gitignore` 排除 `about-me.md`/`my-config.md` 及开发元数据目录；若已提交需清Git历史
2. **B2**：`validate-program.py` 的 `check_company_facts` 改为从 `about-me.md` 动态提取特征
3. **S1**：`quality_gate.py` 路径改为相对推导或读环境变量
4. **S2（启动）**：为 `validate-finding.py` 的blocker逻辑和 `phase_gate.py` 的阶段流转写首批单元测试

### 近期排期（1个月内）
5. **B3**：三处文档"10条"改"11条"
6. **B4**：舞弊场景加举报材料隔离 + force跳过额外记录
7. **B5**：用2个项目跑通跨项目查询全链路
8. **S3**：所有裸except改为记录日志/uncertainties
9. **S4**：CLAUDE-project.md补齐缺失章节
10. **S5**：bump-version.py增加去重
11. **S6**：修复模块级print、裸except、GEB技能漂移
12. **S7**：统一配置字段名

### 长期演进
13. **B6**：完整审计主题端到端跑通验证
14. **B7**：OPS.md增补AI使用边界
15. **S8**：目录整理

---

*本报告基于静态审查，未运行动态测试。标注"设计层面"的判断未经实战验证。建议结合一次真实审计的端到端跑通，校验本报告的发现。*

---

## 修正与补充（读完 setup-project.ps1 / update-project.ps1 后）

> 初版报告未读部署脚本，导致部分判断偏离实际。读完两个脚本（共600行）后修正如下。

### 部署架构的正确理解

`setup-project.ps1` 的部署逻辑揭示了一个关键事实：**`audit-topics/`、`memory/`、`internal-audit-workspace/` 在生产项目里是空目录**（第191行 `$DATA_DIRS`，只 `mkdir` 不拷贝）。也就是说：

- 源仓库里的 `about-me.md`、`my-config.md` **不会部署到生产项目**——它们是开发/样例数据
- 生产项目里这些文件由用户自行填写
- `CLAUDE-project.md` 精简版是**设计意图**（脚本注释明写 "stripped dev-only content"），不是疏忽——生产项目不需要知道"怎么部署的"
- 部署脚本里的 `$GOLD = "D:\Nut\..."` 硬编码**合理**——这是开发机上的部署工具，不是被部署的代码

### 逐条修正

| 编号 | 原判断 | 修正后 | 理由 |
|------|--------|--------|------|
| **B1** | 🔴高 | 🟡中 | `about-me.md` 不部署到生产（setup只建空目录）。源仓库里的是开发样例数据。风险降为"若源仓库上Git需脱敏+gitignore"，而非"生产数据泄露" |
| **S4** | 🟡中 | 🟢低 | CLAUDE-project.md 精简是设计意图，大部分缺失内容（部署架构/L3规则/Rules loading）生产确实不需要。但"任务完成闸机"这条纪律被一起精简掉了，生产也适用，建议补回 |
| **B2** | 🔴高 | 🔴高 **不变** | `validate-program.py` 的 `company_markers` 是**代码逻辑**硬编码，stable模式下照搬拷贝，换公司审计照样误报。部署解决不了代码内部的硬编码 |
| **S1** | 🔴高 | 🔴高 **不变，理由更精确** | `quality_gate.py` 的 `D:/Nut/...` 路径比原先判断的更严重：不只是"换机器失效"，而是**即使同一台机器，它读的是源仓库的 my-config.md 而非项目自己的配置**——逻辑错误，不是路径问题 |

### 修正后风险统计

| 维度 | 高风险 | 中风险 | 低风险 |
|------|:------:|:------:|:------:|
| 业务角度 | 1 | 4 | 2 |
| 系统工程 | 2 | 4 | 2 |
| **合计** | **3** | **8** | **4** |

### 部署架构值得肯定的设计

读完两个脚本后，补充肯定以下设计：
1. **版本锁 + 增量升级**：`VERSION.lock.json` 锁定部署版本，`update-project.ps1` 对比diff并按风险分级（🔴schema/data、🟡config/skill、🟢script/doc），用户确认后才升级——审计项目的行为可追溯、可回滚
2. **数据目录隔离**：`update-project.ps1` 第6行注释 "Never touches audit-topics/, memory/, internal-audit-workspace/"——升级只动工具不动数据，审计工作产物安全
3. **备份轮转**：保留最近3次升级备份，自动清理旧备份
4. **junction/stable双模式**：开发实时同步、生产锁定版本，定位清晰

### 修正后立即修清单

1. ~~B1: .gitignore 排除 about-me.md~~ → 改为：源仓库放 `about-me.template.md`（脱敏），真实样例 gitignore
2. **B2**: `check_company_facts` 改为从项目 `about-me.md` 动态提取特征（部署照搬解决不了，必须改代码）
3. **S1**: `quality_gate.py` 路径改为读项目自身配置（`Path(__file__)` 相对推导），不能指向源仓库
4. **S2**: 写首批单测（不变）
