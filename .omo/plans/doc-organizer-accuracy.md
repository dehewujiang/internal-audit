# doc-organizer-accuracy - Work Plan

## TL;DR (For humans)

**你要得到什么**：document-organizer 的两个核心问题解决——跨章节控制点不再丢碎片、行业基准表从"给人看的参考"升级为"给 LLM 用的可执行对照表"。

**做法**：
1. **行业基准表全面重写**（这次的核心）：从 94 行的备忘录升级为结构化控制维度清单。每个业务领域拆成 ≥3 个控制维度，每个维度标注控制类型（AP/SS/RC/DR）。补充 5 个 Flan 特有遗漏领域。紧固件/冲焊件专项基准补齐 APQP、表面处理、螺纹检测、边角料核算等。与 about-me.md 的实际数据对齐。
2. 分析流程从"逐段扫描"改为"先建目录再写详情"——LLM 第一遍拿行业基准表做对照，建"业务对象索引"（哪些段落讲的是同一件事），第二遍拿完整上下文写控制点。

**不改什么**：不重写 document-organizer 的核心逻辑。不新增 skill。只改 document-organizer 内部文件。

**工作量**：约 1.5 天。T1.1 是重头（行业基准表重写），T1.2/T1.3 较轻松。

## Scope

**IN**:
- `document-organizer/references/industry_benchmarks.md` — 重构为可执行清单
- `document-organizer/references/workflow.md` — 新增两遍法流程
- `document-organizer/SKILL.md` — 更新工作流程引用

**OUT**: 不重写控制点提取算法。不改 validate-policy-analysis.py。不新增 Python 脚本。不改其他 skill。

## Execution strategy

单波次：行业基准升级 + 两遍法流程，三个任务可串行（后两个依赖第一个）：

```
T1.1 → T1.2 → T1.3
```

---

## Todos

---

#### T1.1: industry_benchmarks.md — 全面重写为可执行控制维度清单

**References**: `D:\Nut\00_my_digital\12_AGI\skills\internal-audit\document-organizer\references\industry_benchmarks.md`

**Changes**:

1. **全面重写行业基准表**。从"给人看的备忘录"升级为"给 LLM 用的可执行对照表"，包含以下五个改动：

**改动 A：每个 P0/P1 领域拆成控制维度清单**

每个领域 ≥3 个控制维度，每个维度标注控制类型（AP审批/SS职责分离/RC定期检查/DR文档记录）和应覆盖的关键点。示例格式：

```markdown
### 存货管理（P0）
| 控制维度 | 控制类型 | 应覆盖的关键点 |
|---------|---------|--------------|
| 入库管理 | 审批(AP) + 记录(DR) | 入库单审批层级、质检前置、入库台账 |
| 出库管理 | 审批(AP) + 记录(DR) | 出库申请-审批-发货三步、出库单与订单匹配 |
| 盘点管理 | 定期检查(RC) + 职责分离(SS) | 盘点频率（月/季）、监盘人独立于仓管、差异分级处理 |
| 呆滞料管理 | 定期检查(RC) | 呆滞定义标准（库龄阈值）、定期清理机制、责任归属 |
| 保管管理 | 职责分离(SS) + 记录(DR) | 仓管与记账分离、出入库台账、温湿度/防锈环境控制 |

### 采购管理（P0）
| 供应商准入 | 审批(AP) | 新供应商评估标准、准入审批层级、样品验证 |
| 采购审批 | 审批(AP) | 金额分级审批（≤5万/5-50万/>50万）、紧急采购例外流程 |
| 价格管理 | 审批(AP) + 记录(DR) | 比价/议价留痕、价格审批独立于采购员 |
| 采购验收 | 职责分离(SS) + 记录(DR) | 采购人与验收人分离、验收标准量化、不合格品处理 |
| 采购付款 | 审批(AP) + 职责分离(SS) | 付款审批与采购审批分离、发票-订单-验收单三单匹配 |
```

**改动 B：补充 5 个 Flan 特有遗漏领域**

```markdown
### 多工厂/子公司管控（P1 — 新增）
| 控制维度 | 控制类型 | 应覆盖的关键点 | 对应 about-me 事实 |
|---------|---------|--------------|-----------------|
| 采购权限划分 | 审批(AP) | 各子公司独立采购权限的范围和金额上限、集团统采 vs 子公司自采 | 4 家子公司（宁波长盛/长华、武汉长源、广东长华） |
| 资金调度 | 审批(AP) + 记录(DR) | 子公司间资金调拨审批、银行账户统一监控 | 4 家独立法人 |
| 存货调拨 | 审批(AP) + 记录(DR) | 子公司间存货调拨的定价和审批 | 紧固件+冲焊件跨工厂协作 |
| 报表合并 | 记录(DR) | 关联交易抵消、统一会计政策 | 上市企业合并报表要求 |

### 客户专用件管理（P1 — 新增）
| 模具产权归属 | 记录(DR) | 客户付费模具 vs 自购模具的台账分离 | 前五客户 62.24% |
| 专用件标识 | 记录(DR) | 客户专用件号、专用包装标识、防止混用 | 整车厂多客户并行供货 |
| 客户资产隔离 | 职责分离(SS) | 客户资产物理隔离、定期盘点客户模具 | 客户审计要求 |

### 应收账款/信用管理（P1 — 新增）
| 客户信用评估 | 审批(AP) | 新客户信用评估、授信额度的审批流程 | 年营收 21 亿 |
| 账期管理 | 定期检查(RC) | 应收款逾期天数分级预警、催收流程 | 客户集中度 62.24% |
| 坏账计提 | 定期检查(RC) + 记录(DR) | 坏账计提标准、单独计提和组合计提的规则 | 上市企业财务合规 |

### 钢材价格波动管理（P1 — 新增）
| 价格传导机制 | 审批(AP) | 钢材价格变动与客户调价公式的关联审批 | 原材料：线材、板材 |
| 成本核算 | 记录(DR) | 标准成本 vs 实际成本差异追踪 | 钢材占总成本比例高 |
| 库存策略 | 审批(AP) | 安全库存量的设定和调整审批 | 价格波动大时囤货/去库存决策 |

### 生产计划/产能管理（P2 — 新增）
| 排产管理 | 记录(DR) | 四工厂排产协同、产能利用率监控 | 4 家工厂，紧固件+冲焊件 |
| 交期管理 | 定期检查(RC) | 客户订单交期达成率统计和延迟根因分析 | 整车厂 JIT 交付要求 |
```

**改动 C：紧固件行业专项基准补齐**

在现有 4 个控制领域基础上新增：

```markdown
### 紧固件行业（扩展）
| 控制领域 | 应建立的控制 | 基准要求 |
|---------|-------------|---------|
| 表面处理 | 外协电镀/达克罗/磷化质量控制 | 外协方工艺参数记录、每批次来料检验盐雾试验 |
| 螺纹检测 | 在线检测或抽检标准 | 检测频率、不合格品处置、检测设备校准 |
| 线材批次追溯 | 材料批号→产品批号→客户发货号全链路追溯 | 一个批号有质量问题能追溯到所有受影响产品 |
| 热处理 | （保留现有）温度/时间自动记录，偏差报警 |
```

**改动 D：冲焊件行业专项基准补齐**

在现有 4 个控制领域基础上新增：

```markdown
### 冲焊件行业（扩展）
| 焊接参数监控 | 电流/电压/速度实时记录和异常报警 | 参数偏离立即停线、焊接试样定期送检 |
| 模具寿命管理 | 冲压模具冲次计数和预防性维护 | 冲次计数器、达寿命自动锁定、维修记录 |
| 边角料核算 | 投料量-产出量-废料量物料平衡 | 理论产出率与实际产出率偏差超过 X% 报警 |
```

**改动 E：汽车行业通用专项基准（两种产品共有）**

```markdown
### 汽车行业通用专项
| 控制领域 | 应建立的控制 | 基准要求 |
|---------|-------------|---------|
| APQP/PPAP | 产品质量先期策划和生产件批准程序 | 每款新零件需完成 PPAP 提交并获得客户批准 |
| IMDS/CAMDS | 材料数据申报系统 | 每款产品需在客户指定系统中完成材料成分申报 |
| EDI 客户对接 | 电子数据交换系统 | 订单接收、发货通知 ASN、结算对账全部通过客户 EDI |
```

2. **新增"公司锚定参数"章节**。从 about-me.md 提取关键数据作为控制维度的上下文：

```markdown
## 公司锚定参数

本文档中的控制维度需结合以下公司实际数据调整阈值和关注点：

| 参数 | 值 | 影响的控制维度 |
|------|-----|--------------|
| 年营收 | 21 亿 | 采购审批金额区间、应收账款单项重要性水平 |
| 子公司数 | 4 家 | 多工厂管控、存货调拨、关联交易 |
| 前五客户集中度 | 62.24% | 客户专用件管理、应收账款信用控制 |
| 员工数 | ~2000 | 职责分离可行性（人数少时合理的一人多岗范围） |
| 主要原材料 | 钢材（线材+板材） | 价格波动管理、成本核算 |
| ERP 系统 | SAP | IT 一般控制、权限管理、变更管理 |
```

3. 新增"行业基准版本管理"章节：

```markdown
## 行业基准版本管理

本文档是 document-organizer 提取完整性的对照基准。每次以下事件发生后应审查本基准是否需要更新：
- 公司新增业务线或工厂
- 主机厂客户新增特殊要求
- 行业监管要求更新（如 IATF 16949 改版）
- 审计发现持续暴露某领域控制缺失

最后更新：[日期]
最后审查：[日期]
```

**Acceptance**: 
- 每个 P0 领域 ≥3 个控制维度，P1 领域 ≥3 个控制维度，每个维度标注控制类型
- 5 个新增领域全部出现
- 紧固件专项基准 ≥7 个控制领域
- 冲焊件专项基准 ≥7 个控制领域
- 汽车行业通用专项 ≥3 个控制领域
- "公司锚定参数"章节存在且引用 about-me.md 数据
- 版本管理章节存在

**QA**: 
- Happy: 统计 P0 领域数 × 平均控制维度数 ≥ 3；P1 领域总数 ≥ 10
- Happy: 紧固件行业专项控制领域 ≥ 7；冲焊件 ≥ 7；汽车通用专项 ≥ 3
- Failure: 任何 P0 领域控制维度 < 3 → 不通过
- Failure: 5 个新增领域（多工厂管控、客户专用件、应收/信用、钢材价格、生产计划）缺失任一个 → 不通过
- Failure: "公司锚定参数"章节未引用 about-me.md 的具体数字 → 不通过

**Commit**: `feat: rewrite industry_benchmarks as executable control dimension checklist`

---

#### T1.2: workflow.md — 新增两遍法流程

**References**: `D:\Nut\00_my_digital\12_AGI\skills\internal-audit\document-organizer\references\workflow.md`

**Changes**:

在现有"逐份深度分析"步骤之后、"流程重建"之前，插入"**Step X：业务对象索引建立**（新增）"：

```markdown
## Step X：业务对象索引建立（新增 — 始终执行）

**目的**：解决控制点规则分散在多个章节时 LLM 逐段分析无法拼接的问题。

### 第一遍：LLM 建索引

读完制度全文后，不提取控制点，只输出"业务对象索引"：

| 业务对象 | 涉及章节 | 控制类型 | 摘要 |
|---------|---------|---------|------|
| 采购审批 | 3.1, 5.3, 7.2 | AP | 正常流程(3.1)+紧急例外(5.3)+设备采购特例(7.2) |
| 盘点管理 | 6.1, 6.4, 附录B | RC+DR | 月度盘点(6.1)+差异处理(6.4)+盘点表模板(附录B) |
| ... | ... | ... | ... |

**索引规则**：
- 按"业务对象"归并，不是按章节归并
- 同一业务对象的所有相关段落必须在同一行
- 如果某业务对象只有一个段落提及，仍需列入索引（完整性要求）
- 控制类型列标注该业务对象涉及的控制类型（对照 control_taxonomy.md 的 AP/SS/RC/DR 分类）

### 第二遍：逐业务对象分析

对索引中的每个业务对象：
1. 从原文中提取所有相关段落的完整原文
2. 拼成一个文本块
3. 一次性喂给 LLM 分析该业务对象下的完整控制点

**输出**：每个业务对象产出一个控制点分析块，LDM 拿到的是完整上下文，不是碎片。
```

同时更新顶部的流程图为：

```
批量扫描文件夹
↓
建立"文件-章节-业务领域"索引
↓
版本识别
↓
【新增】业务对象索引（第一遍：只归类，不写详情）
↓
【新增】逐业务对象分析（第二遍：拿完整上下文写控制点）
↓
流程重建（提取隐含控制）
↓
关键词搜索 → 标注关键要求
↓
...
```

**Acceptance**: workflow.md 完整描述了两遍法流程，LLM 能据此执行。

**Commit**: `feat: add two-pass business object indexing to document-organizer workflow`

---

#### T1.3: SKILL.md — 更新工作流程引用

**References**: `D:\Nut\00_my_digital\12_AGI\skills\internal-audit\document-organizer\SKILL.md`

**Changes**:

在"工作流程"章节（约 line 68-70），将现有的简单引用更新为包含两遍法的说明：

```markdown
## 工作流程

详见 [references/workflow.md](./references/workflow.md)

**分析模式**：采用两遍法——
1. 第一遍：LLM 快速扫读全文，建立"业务对象索引"（哪些章节在讲同一件事）
2. 第二遍：对每个业务对象，把散落在各章节的原文归拢后一次性交给 LLM 分析，确保拿到的是完整上下文而非碎片

**分治法（6+文件时必须使用）**：
```

**Acceptance**: SKILL.md 明确描述了分析模式的变化。

**Commit**: `feat: update SKILL.md to reflect two-pass analysis mode`

---

## Final verification wave

全部完成后：

F1: industry_benchmarks.md — 确认每个 P0 领域的控制维度 ≥ 3，P1 领域总数 ≥ 10
F2: industry_benchmarks.md — 确认 5 个新增领域全部出现
F3: industry_benchmarks.md — 确认紧固件专项 ≥7、冲焊件专项 ≥7、汽车通用专项 ≥3
F4: industry_benchmarks.md — 确认"公司锚定参数"章节存在且引用 about-me.md 数据
F5: workflow.md — grep 确认"业务对象索引"和"两遍法"出现
F6: SKILL.md — 确认引用两遍法分析模式

## Commit strategy

```
feat: rewrite industry_benchmarks as executable control dimension checklist
feat: add two-pass business object indexing to document-organizer workflow
```

## Success criteria

1. industry_benchmarks.md：P0 领域 ≥3 控制维度，P1 领域 ≥3 控制维度，5 个新增领域全覆盖
2. industry_benchmarks.md：紧固件专项 ≥7 控制领域，冲焊件专项 ≥7，汽车通用 ≥3
3. industry_benchmarks.md："公司锚定参数"章节含 about-me.md 的 6 个关键数据点
4. workflow.md 包含完整的两遍法流程描述
5. SKILL.md 明确引用两遍法分析模式
