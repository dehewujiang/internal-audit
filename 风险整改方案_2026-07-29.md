# 风险整改方案（优化版）

> 生成日期：2026-07-29 | 优化日期：2026-07-29
> 变更说明：R04 纠正诊断——覆盖度校验基础设施（ProgramIR + `--ir` 模式）已存在，缺的是工作流接入。R07 对齐 ProgramIR 体系。R08 补齐"有意变更 vs 无意漂移"机制。补充总工作量预估和改动窗口期。

---

## 核验结论速览

| 风险 | 诊断准确性 | 关键修正 |
|:---|:---|:---|
| R01 | ✅ 准确 | 无 |
| R02 | ✅ 准确 | 无 |
| R03 | ✅ 准确 | 无 |
| R04 | ❌ 需要修正 | 覆盖度校验基础设施**已存在**（`program_ir_parser.py` + `validate-program.py --ir`），缺的是接入 Skill 工作流 |
| R05 | ✅ 准确 | 补充字段规范来源 |
| R06 | ✅ 准确 | 无 |
| R07 | ❌ 需要修正 | 与 R04 联动改为 ProgramIR 方案，增加修复闭环 |
| R08 | ⚠️ 缺关键机制 | 补充"有意变更 vs 无意漂移"区分逻辑 |
| R09 | ✅ 准确 | 无 |

---

## 影响范围总估价

| 阶段 | 改动文件数 | 预计工作量 | 风险窗口 |
|:---|:---|:---|:---|
| 阶段一 | 6-8 个文件 | 2-3 小时 | 低——修已存在的矛盾，不引入新逻辑 |
| 阶段二 | 4-5 个文件 | 3-5 小时 | 中——涉及脚本新增和工作流变更 |
| 阶段三 | 3-4 个文件 | 4-8 小时 | 低——开发工具，不影响审计产出 |

> **窗口期建议**：阶段一、二在下一个审计项目启动前完成。阶段三可在日常迭代中穿插。

---

## 实施优先级总览

- **第一阶段（立即）**：R01 - R03，修复已存在的规则矛盾和丢失
- **第二阶段（尽快）**：R04 - R07，补齐验证闭环核心缺口
- **第三阶段（排期）**：R08 - R09，开发效率和长期质量保障

---

## 🔴 高优先级

### R01：高风险 finding 证据等级门槛矛盾

- **状态**：`cceer_standards.md` 第 108 行写"A 级或 B 级"，`SKILL.md` 第 596 行和 `root_cause_framework.md` 第 136 行写"A 级或 E 级"——两套规则冲突。`evidence_grading.snap` 快照也记录为 A+E。
- **根因**：[推测] 某次编辑时误将 A+E 改为 A+B，未同步其他文件。
- **影响范围**：所有高风险 finding 的证据判定。LLM 读到不同文件会遵循不同标准，同一 finding 可能在不同阶段得到矛盾判定。
- **解决方案**：统一为 A 或 E（更严格）。B 级（截图）可被 PS/HTML 篡改，不应作为高风险 finding 的独立支撑。
- **实施步骤**：
  1. 确认用户最终决定：A+E（严格）还是 A+B（宽松）。默认推荐 A+E。
  2. 修改 `cceer_standards.md` 第 108 行，把 "A 级或 B 级" 改为 "A 级或 E 级"
  3. 全文搜索所有含"高风险 finding"+"证据"的位置，确保全部统一：
     - `audit-execution-assistant/SKILL.md` 第 596 行（已是 A+E，确认无需改动）
     - `audit-execution-assistant/references/root_cause_framework.md` 第 136 行（已是 A+E，确认无需改动）
     - `audit-execution-assistant/references/cceer_standards.md` 第 108 行（需修改）
  4. 更新 `evidence_grading.snap` 快照（随 R03 批处理）
- **预期效果**：消除模型读取时的规则歧义，所有高风险 finding 按统一门槛判定
- **责任方**：开发者

---

### R02：对抗验证定量阈值丢失

- **状态**：`adversarial_validation.snap` 第 20-21 行记录了 `EXPOSED 超过 30% → 审计程序需要加强`、`COVERED 不足 50% → 考虑重新设计程序`，但当前 `internal-audit-program-generator/SKILL.md` 第 267-287 行"裁判阶段"仅有 COVERED/PARTIALLY_COVERED/EXPOSED 的三级定义，**无定量判定线**。
- **根因**：[推测] 某次 SKILL.md 重写时，三段式判定被保留但量化阈值被省略。
- **影响范围**：所有激活轨道 B 的审计——对抗验证失去可自动判定的好坏分数线，从"可量化判定"退化为"全凭人眼判断"。
- **解决方案**：在 `internal-audit-program-generator/SKILL.md` 的 Step 3.7（裁判阶段）末尾补回两条定量判定标准。
- **实施步骤**：
  1. 在 Step 3.7 "裁判阶段"的"强制规则"段落之前，插入判定标准：
     ```markdown
     ### 定量判定标准
     - EXPOSED（风险敞口）占比 > 30% → 审计程序需要加强
     - COVERED（已覆盖）占比 < 50% → 考虑重新设计程序
     ```
  2. 同步更新 `adversarial_validation.snap` 快照（随 R03 批处理）
  3. 验证：用 `fixtures/样本_存货管理` 跑一次轨道 B，确认阈值出现在输出中
- **预期效果**：轨道 B 的对抗验证结果恢复可量化判定
- **责任方**：开发者

---

### R03：5 份 Prompt 快照过期

- **状态**：核验确认——`evidence_grading.snap` 写 A+E 但源文件是 A+B（关联 R01），`adversarial_validation.snap` 有 30%/50% 阈值但源文件没有（关联 R02）。另外 3 份（`cceer_chain.snap` / `root_cause_challenge.snap` / `intuition_engine.snap`）与源文件可能有偏差但未经逐份对比。
- **影响范围**：后续任何 Skill 改动都无法做回归对比——快照是唯一可以 diff 的"标准照"，快照和源文件不一致时改坏了也不知道。
- **解决方案**：以当前源文件为准，重写全部 5 份快照，并在每份快照头部标注更新日期和变更说明。
- **实施步骤**：
  1. **先修 R01 和 R02**——快照更新必须在源文件矛盾消除之后进行
  2. 逐份对比源文件与快照，重写以下 4 份：
     - `cceer_chain.snap` — 对比 `audit-execution-assistant/SKILL.md` Step 3
     - `root_cause_challenge.snap` — 对比 `audit-execution-assistant/references/root_cause_framework.md`
     - `adversarial_validation.snap` — 对比 `internal-audit-program-generator/SKILL.md` Step 3.7 + `references/step3_program_generation.md`
     - `evidence_grading.snap` — 对比 `audit-execution-assistant/references/cceer_standards.md`
  3. `intuition_engine.snap` 仅更新头部的日期标注
  4. 每份快照头部格式统一为：
     ```
     # [快照名]
     # 提取自 [源文件路径]
     # 日期: 2026-07-29 | 变更: [简述本次变更]
     ```
  5. 提交时在 commit message 中附变更说明（哪些条目有实质变化）
- **预期效果**：恢复快照的"标准照"职能，后续改动可对比 diff
- **责任方**：开发者

---

## 🟡 中优先级

### R04：审计程序覆盖度校验未接入工作流（G1）⚠️ 诊断已修正

- **原诊断错误**："validate-program.py 只查格式，不查覆盖"——此描述不准确。实际情况：
  - `check_risk_coverage()` 已存在（第 86 行），做正则粗扫（数 R01/R02 编号 vs A1.1/B2.3 程序编号）
  - `--ir` 模式下的 `check_ir_coverage_rate()` 已存在（第 213 行），做结构化覆盖检查（覆盖率 < 80% → block）
  - `program_ir_parser.py` 已存在，能将审计程序 Markdown 解析为 ProgramIR JSON（含 `risk_register`、`coverage`、`uncovered_risks`）
- **真正的缺口**：这套基础设施**没有接入 Skill 工作流**。`internal-audit-program-generator/SKILL.md` 的 Step 5 质量评估是纯 LLM 推理检查，不调用 `validate-program.py --ir`。程序生成后无人触发脚本校验。
- **影响范围**：所有审计——可能产出格式完美但内容缺漏的程序，且不会被脚本自动发现。
- **解决方案**：在 program-generator 工作流的 Step 4（输出）之后，增加 Step 4.5：调用 `program_ir_parser.py` 解析 Markdown → 调用 `validate-program.py --ir` 做结构化校验 → 不通过则阻断。
- **实施步骤**：
  1. 修改 `internal-audit-program-generator/SKILL.md`，在 Step 4 和 Step 5 之间插入 Step 4.5：
     ```markdown
     ## Step 4.5: 程序结构化校验（脚本闸机）

     1. 运行 `python _shared/scripts/program_ir_parser.py <程序MD文件> --out internal-audit-workspace/program_ir.json`
     2. 运行 `python _shared/scripts/validate-program.py <程序MD文件> --ir internal-audit-workspace/program_ir.json`
     3. 若有 blocker → 回到 Step 3 修复后重新生成
     4. 若仅有 warning → 记录在 Step 5 的质量评估中一并考虑
     ```
  2. 修改 Step 5 质量评估的开头，增加一句："已通过 Step 4.5 脚本闸机（覆盖度/判定标准量化/数据来源比例检查）"
  3. 不新建 `risks_identified.json`——ProgramIR 的 `risk_register` 字段已承载相同信息，无需重复建设。
- **为什么不用原方案的 `risks_identified.json`**：ProgramIR 已从 Markdown 解析出 `risk_register`（含 risk_id + title + desc + type），并在 `coverage` 字段中计算了覆盖率和未覆盖列表。另建一个 JSON 等于维护两套数据——违反单一事实源原则。
- **预期效果**：风险覆盖面缺失在输出前被脚本硬拦截，与 Step 5 的 LLM 推理检查形成互补（脚本管"有没有"，LLM 管"好不好"）
- **责任方**：开发者

---

### R05：_evidence_catalog.json 缺独立 schema 校验器（G2）

- **状态**：`validate-catalog.py` 不存在。catalog 由 `evidence_catalog.py` 生成，但无独立校验器验证读取时的结构完整性。
- **影响范围**：所有审计的执行阶段——catalog 被手动编辑或程序错误损坏后，execution-assistant 可能误判证据缺失，导致 finding 被错误标记为"证据不足"或放行不完整证据。
- **解决方案**：编写 `validate-catalog.py`，校验 catalog JSON 的结构完整性。
- **实施步骤**：
  1. 从 `evidence_catalog.py` 源码和 `_evidence_catalog.json` 实例中提取完整字段规范：
     - 顶层字段：`schema_version`, `project_name`, `project_path`, `generated_at`, `slots`
     - 槽位必需字段：`slot_id`, `program_id`, `evidence_desc`, `required_level`, `status`, `matched_files`
     - `matched_files[]` 必需字段：`file`, `match_method`, `match_confidence`
  2. 编写 `_shared/scripts/validate-catalog.py`：
     - 结构校验：JSON 可解析、必需字段存在、字段类型正确
     - 逻辑校验：`slot_id` 无重复、`matched_files[].file` 路径格式合法、`status` 值在枚举范围内
     - 交叉校验：`slot_id` 引用的 `program_id` 是否在程序范围内（可选，留扩展点）
  3. 在 `audit-execution-assistant/SKILL.md` 的读取 catalog 步骤后增加调用指令
  4. 同时挂入 `audit_gate.py` 的 precheck——catalog 损坏时阻止进入执行阶段
- **预期效果**：catalog 被意外损坏时在执行阶段入口被立即发现
- **责任方**：开发者

---

### R06：index.json 缺校验脚本（G3）

- **状态**：`index_schema.md` 规范存在（`audit-execution-assistant/references/index_schema.md`），但 `validate-index.py` 不存在。
- **影响范围**：报告生成阶段——index.json 与实际 finding 文件不一致时，汇总统计数据（finding 数量、风险等级分布、证据完成率）全部出错。
- **解决方案**：编写 `_shared/scripts/validate-index.py`，校验字段完整性 + 交叉核验。
- **实施步骤**：
  1. 基于 `index_schema.md` 定义校验规则（字段名、类型、必需性）
  2. 编写 `validate-index.py`：
     - 结构校验：JSON 可解析、必需字段齐全、字段类型正确
     - 交叉比对：index 中的 finding 条目数 vs `findings/` 目录实际 `.json` 文件数
     - 闭合检查：index 中每个 finding 的 `finding_id` 与对应 JSON 文件的 `finding_id` 是否一致
  3. 在 `internal-audit-report-generator/SKILL.md` 的读取 index 步骤前调用
  4. 发现漂移时 action=block，输出差异明细（index 有但目录没有 / 目录有但 index 没有）
- **预期效果**：index.json 漂移被自动检测并阻断，避免报告汇总数据出错
- **责任方**：开发者

---

### R07：风险清单→程序覆盖的内部推理断点（G4）⚠️ 方案已对齐 R04

- **原方案问题**：依赖新造的 `risks_identified.json`，与现有 ProgramIR 体系重复。且只设计了"事后检测"，未设计"发现遗漏后怎么修"。
- **状态**：Step 2 的风险清单在 LLM 推理内存中，Step 3 生成程序时可能遗忘部分风险。这是 LLM 工作流的固有缺陷——上下文窗口内的信息衰减。
- **影响范围**：所有审计——程序遗漏时无人知晓，直到执行阶段才发现测试不完整。
- **解决方案**：三重屏障——生成前自检 + 生成后脚本硬拦截 + 修复闭环。

  **屏障一（生成前自检）**：Step 3 开头增加强制指令：
  ```markdown
  开始生成程序前，将 Step 2 识别出的所有 risk_id 列在输出中（格式：`[自检] 待覆盖风险: R01, R02, ...`）。
  每生成完一个轨道的程序后，在输出中再次列出该轨道已覆盖的 risk_id。
  全部轨道完成后，输出 `[自检] 覆盖完整性确认: N/N 风险均已分配程序`。
  ```
  作用：让 LLM 在生成过程中"看见"完整清单，减少遗忘。这不是防漏的充分条件，但能显著降低遗漏率。

  **屏障二（生成后脚本拦截）**：R04 修复后，Step 4.5 调用 `validate-program.py --ir` 做结构化覆盖检查。覆盖率 < 80% 或有未覆盖风险无理由 → block。

  **屏障三（修复闭环）**：脚本拦截后的处理流程：
  1. 查看 `program_ir.json` 中 `coverage.uncovered_risks` 字段，获取遗漏的风险 ID 和名称
  2. 回到 Step 3，针对遗漏的风险重新生成对应轨道的程序段（不是全部重来）
  3. 重新跑 Step 4.5 脚本校验 → 通过后进入 Step 5
- **预期效果**：三重屏障（LLM 自检 + 脚本拦截 + 修复闭环），覆盖遗漏率趋近于零
- **责任方**：开发者（屏障一、二与 R04 合并实施，屏障三写入 SKILL.md 的异常处理段落）

---

## 🟢 低优先级

### R08：开发环境缺闸机——快照对比未强制执行

- **状态**：改 Skill 的 MD 文件后无任何自动检查。
- **影响范围**：所有 Skill 的迭代维护——关键规则被无意改动后无感知，质量退化只能靠真实审计事故反向发现。
- **解决方案**：在 git pre-commit hook 中挂载快照对比脚本，并在脚本内实现"有意变更 vs 无意漂移"的智能区分。
- **实施步骤**：
  1. **前提**：R03 完成（快照本身先恢复准确）
  2. 编写 `compare-snapshots.sh`：
     - 逐份 diff 快照与源文件的关键段落（非逐行，用段落哈希）
     - **区分逻辑**：检查本次 commit 是否同时修改了快照文件（`.snap`）和对应的源文件（`.md`）
       - 源文件和快照**同时被修改** → 判定为"有意变更"→ 静默通过
       - 源文件**被修改但快照未改动** → 判定为"可能漂移"→ 弹出警告
       - 输出格式：`⚠️ [快照名] 源文件已变更但快照未同步。如为有意变更请同步更新快照；如为无意漂移请回滚源文件。跳过本次检查：git commit --skip-snap-check`
  3. 挂到 `.git/hooks/pre-commit`（通过 `update-config` skill 或手动）
  4. 提供跳过机制：`git commit --skip-snap-check` 或环境变量 `SKIP_SNAP_CHECK=1`
- **预期效果**：每次提交前自动区分"规则变更"和"规则漂移"，不阻碍正常迭代，但拦截无意改动
- **责任方**：开发者

---

### R09：端到端回归测试缺位

- **状态**：无法自动验证"改了 Skill 后产出质量是否下降"。
- **影响范围**：长期维护——质量退化只能通过真实审计事故反向发现。
- **解决方案**：短期靠人工抽查纪律，长期靠自动化回归框架。本方案诚实承认"没有银弹"。
- **实施步骤**：
  1. **短期（立即）**：建立人工抽查清单
     - 定义触发抽查的改动规模：schema 升级、SKILL.md 重写、references 目录大改、引入新脚本
     - 抽查动作：用一份已完成项目的真实输入（policy-analyses + DRL + about-me），重新跑一次完整流程，对比新旧输出的 finding 数量和风险等级分布
     - 在 commit message 中标注 `已人工回归: [抽查项目名]`
  2. **中期（排期）**：积累标准用例
     - 从已完成的真实审计项目中提取 5-10 份有定论的输入输出对（程序 + finding + 报告）
     - 存入 `tests/fixtures/regression/` 目录，每份含 `input/` + `expected_output/` + `README.md`（说明此用例覆盖的场景）
  3. **长期（排期）**：开发自动化回归框架
     - 框架设计：遍历 `tests/fixtures/regression/` → 用 fixture 的输入重跑对应 Skill → diff 输出与 expected_output → 报告差异
     - 不做全自动断言（LLM 输出本质上是非确定性的），而是做"差异评级"：GREEN（无实质差异）/ YELLOW（输出结构一致但措辞不同）/ RED（关键字段缺失或结论反转）
     - 接入 CI（如果有）
- **预期效果**：短期靠纪律、中期靠半自动、长期靠自动化的渐变过渡。不画饼，每步都有可验收的交付物。
- **责任方**：开发者（长期框架）/ 审计执行者（短期抽查）

---

## 实施顺序建议

```
阶段一（本次）：R01 → R02 → R03          （修错误，恢复基线）
阶段二（下次）：R04 → R05 → R06 → R07    （R04 是核心——接入 ProgramIR 闸机；R05/R06 补齐校验脚本；R07 随 R04 联动完成）
阶段三（排期）：R08 → R09                （开发效率和质量长线，R08 需 R03 先完成）
```

## 依赖关系图

```
R01 ──┬──→ R03（快照更新依赖 R01 先统一规则）
      │
R02 ──┘

R03 ──→ R08（快照对比 hook 依赖快照先恢复准确）

R04（ProgramIR 闸机接入）──→ R07（覆盖修复闭环依赖 R04 的脚本拦截）

R05（catalog 校验）─ 独立
R06（index 校验）  ─ 独立

R09 ─ 独立（可随时启动短期抽查）
```

---

## 本次优化的主要变更

1. **R04 诊断修正**：从"缺覆盖度检查"修正为"覆盖度检查基础设施已存在（ProgramIR + `--ir`），缺的是接入 Skill 工作流"。废弃 `risks_identified.json` 方案，用现有 ProgramIR 的 `risk_register` 字段替代——不重复造轮子。
2. **R07 方案重写**：对齐 ProgramIR 体系，增加"修复闭环"设计——事后脚本发现遗漏后，不是只报错，而是有明确的回修路径。
3. **R08 补齐关键机制**：增加"有意变更 vs 无意漂移"的区分逻辑——源文件和快照同时改 = 有意，只改源文件不改快照 = 警告。
4. **新增加总工作量预估、改动窗口期建议、依赖关系图**——让方案从"要改什么"升级为"怎么执行"。
