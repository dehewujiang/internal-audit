# 数据流转整改验证计划

> 日期：2026-07-22
> 范围：5 批次 16 项整改的全面验证
> 执行：整改完成后逐项执行

---

## 一、验证目标

| 维度 | 目标 |
|------|------|
| 功能正确性 | 所有修复后的模块在正常输入下产生正确输出 |
| 数据完整性 | 跨模块数据流转无字段丢失、格式变形、内容截断 |
| 异常处理 | 边界输入、缺失输入、恶意输入下系统不崩溃且给出可诊断的错误 |
| 回归安全 | 已正常运行的功能不被修复破坏 |
| 集成一致性 | 闸机、白名单、验证脚本与实际数据格式一致 |

---

## 二、验证范围

| 包含 | 不包含 |
|------|--------|
| 批次 A-E 所有 22 项问题修复后的代码 | 原始评估报告中的业务合规整改（已单独验证通过） |
| validate-finding.py 新 schema 校验逻辑 | LLM 推理质量（非确定性的 AI 输出不在本次范围） |
| audit_gate.py 参数传递与 postcheck 流程 | 完整的 6 阶段审计运行（超出本次范围，建议后续做端到端测试） |
| data_executor.py 沙箱安全与超时 | OCR 识别质量（依赖 PaddleOCR 安装，网络因素不可控） |
| phase_gate.py 工具白名单更新 | 跨项目查询的相似度检测精度 |
| constitution #10 检查脚本 | — |

---

## 三、测试用例设计

### 批次 A：打通验证链路（6 个测试用例）

#### TC-A1：新 schema finding 通过校验

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 validate-finding.py 能正确校验 finding_schema.md 1.2.0 格式的 JSON |
| **前置条件** | P0-5 + P0-6 已修复 |
| **测试步骤** | 1. 准备一个符合 finding_schema.md 的 finding JSON（含 finding_id/title/risk_level/origin/criteria/condition/cause/recommendation/evidence）<br>2. 运行 `python validate-finding.py <test_finding.json> --exit-on-error`<br>3. 检查退出码和输出 |
| **输入数据** | 按 finding_schema.md 构造的合法 JSON |
| **预期结果** | exit 0，输出 "All checks passed" 或等效通过信息 |
| **通过标准** | exit code == 0 |
| **失败标准** | exit code != 0 或报 "缺少必要字段" |

#### TC-A2：不完整的新 schema finding 被拦截

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证缺少必填字段的 finding 被正确拦截 |
| **前置条件** | 同 TC-A1 |
| **测试步骤** | 1. 准备一个故意缺少 `cause` 和 `evidence` 字段的 finding JSON<br>2. 运行 `python validate-finding.py <bad_finding.json> --exit-on-error` |
| **预期结果** | exit 1，输出指出缺失的具体字段名 |
| **通过标准** | exit code == 1，错误信息含 "cause" 或 "evidence" |
| **失败标准** | exit 0（漏过）或 exit 1 但错误信息不相关 |

#### TC-A3：validate-finding.py 文件发现正确

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证批量校验能找到 F-YYYY-NNN.json 格式的文件 |
| **前置条件** | 同 TC-A1 |
| **测试步骤** | 1. 创建临时目录，放入 `F-2026-001.json` 和 `FIND-001.json`<br>2. 运行 `python validate-finding.py --findings-dir <tmpdir>`<br>3. 观察找到了哪些文件 |
| **预期结果** | 找到 F-2026-001.json，不找 FIND-001.json（旧命名） |
| **通过标准** | 输出中列出 F-2026-001.json 的校验结果 |
| **失败标准** | 找不到 F-2026-001.json，或错误匹配了 FIND-001.json |

#### TC-A4：audit_gate.py postcheck 正常执行

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 audit_gate.py 对 finding/政策分析的 postcheck 不再报参数错误 |
| **前置条件** | P0-9 已修复 |
| **测试步骤** | 1. `python audit_gate.py postcheck --action validate_finding --file <pass.json>`<br>2. `python audit_gate.py postcheck --action generate_policy_analysis --file <pass.json>` |
| **预期结果** | 两次调用均不报 "unrecognized arguments: --strict" |
| **通过标准** | 两次调用的 stderr 中无 "unrecognized arguments" |
| **失败标准** | 任一调用报参数错误 |

#### TC-A5：finding-debate 示例 ID 格式一致

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 finding-debate SKILL.md 示例用的 ID 格式与 schema 一致 |
| **前置条件** | P0-4 已修复 |
| **测试步骤** | grep finding-debate/SKILL.md 中 finding_id 的值 |
| **预期结果** | 格式为 F-YYYY-NNN（如 F-2026-001） |
| **通过标准** | 不存在 FIND-NNN 格式的 ID |
| **失败标准** | 仍存在 FIND-001 等旧格式 |

#### TC-A6：finding_schema.md 含辩论字段

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 finding_schema.md 定义了 business_validation 和 debate_sessions |
| **前置条件** | P0-3 已修复 |
| **测试步骤** | grep finding_schema.md 搜索 "business_validation" 和 "debate_sessions" |
| **预期结果** | 两个字段均在 schema JSON 中定义 |
| **通过标准** | 两处各至少 1 个匹配 |
| **失败标准** | 任一字段 0 匹配 |

---

### 批次 B：新模块融入系统（5 个测试用例）

#### TC-B1：data_executor.py 通过 tool-check

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证新脚本通过 phase_gate 工具域白名单 |
| **前置条件** | P0-8 已修复 |
| **测试步骤** | 1. `python phase_gate.py tool-check data_executor.py`<br>2. `python phase_gate.py tool-check audit_gate.py` |
| **预期结果** | 两次调用均 exit 0 |
| **通过标准** | exit code == 0 |
| **失败标准** | exit code == 1（被拦截） |

#### TC-B2：data_executor.py 沙箱拦截恶意导入

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 LLM 生成的恶意代码（import os/system）被沙箱阻止 |
| **前置条件** | P1-15 已修复 |
| **测试步骤** | `python data_executor.py execute -c "import os; os.system('echo hacked'); OUTPUT = None" -d "x=nonexist.csv"` |
| **预期结果** | 返回 error 含 "不允许导入" 或 ImportError |
| **通过标准** | error 字段非 null，含 "不允许导入" 或 "ImportError" |
| **失败标准** | 代码成功执行或系统命令被执行 |

#### TC-B3：data_executor.py 安全代码正常执行

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证合法的 pandas 代码能在沙箱中正常执行 |
| **前置条件** | P1-15 已修复（import 白名单机制） |
| **测试步骤** | `python data_executor.py execute -c "import pandas as pd; df = pd.DataFrame({'a':[1,2,3]}); OUTPUT = df" -d "x=dummy.csv"` |
| **预期结果** | 返回 rows=3, columns=['a'] |
| **通过标准** | rows == 3, error == null |
| **失败标准** | import 被白名单拦截导致报错 |

#### TC-B4：data_executor.py 超时生效（Windows）

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证死循环代码在 Windows 上能被超时机制终止 |
| **前置条件** | P1-14 已修复 |
| **测试步骤** | `python data_executor.py execute -c "while True: pass" -d "x=dummy.csv"` |
| **预期结果** | 30 秒内返回 error 含 "超时" |
| **通过标准** | 执行时间 ≤ 35 秒，error 含 "超时" 或 "timeout" |
| **失败标准** | 执行超过 60 秒未终止 |
| **风险提示** | Windows 上 threading.Timer 方案可能不够强力，如失败需降级为子进程方案 |

#### TC-B5：data_executor.py 8 个预制工具可用

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 LLM 可在代码中调用预制工具函数 |
| **前置条件** | P1-15 已修复 |
| **测试步骤** | `python data_executor.py execute -c "import pandas as pd; df = pd.DataFrame({'金额':[100,200,300]}); result = benford(df, '金额'); OUTPUT = pd.DataFrame([result])" -d "x=dummy.csv"` |
| **预期结果** | benford 函数可调用，返回结果含 observed/expected/anomaly_digits |
| **通过标准** | error == null, summary.columns 含 anomaly_digits 相关字段 |
| **失败标准** | NameError: name 'benford' is not defined |

---

### 批次 C：核心约束代码化（3 个测试用例）

#### TC-C1：mandatory 检查脚本完整覆盖

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 check_mandatory_coverage.py 能正确检测缺失模块 |
| **前置条件** | P0-10 已修复（新增脚本存在） |
| **测试步骤** | 1. 准备测试用的 topic.json（含 3 个 mandatory 模块）<br>2. 在 documents/ 中只放对应 2 个模块的文档<br>3. 运行 `python check_mandatory_coverage.py --topic 测试主题`<br>4. 检查输出 |
| **预期结果** | 输出标记 1 个模块缺失，risk_level="高" |
| **通过标准** | 输出中含 "缺失: 1" 且含对应模块名 |
| **失败标准** | 输出 0 缺失（漏检）或崩溃 |

#### TC-C2：mandatory 检查脚本全匹配

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证所有模块都匹配时输出通过 |
| **前置条件** | 同 TC-C1 |
| **测试步骤** | documents/ 中放入对应全部 3 个模块的文档后重新运行 |
| **预期结果** | 输出 "全部模块已覆盖" |
| **通过标准** | 输出中含 "覆盖: 3/3" 或等效 |
| **失败标准** | 错误报告缺失模块 |

#### TC-C3：存货管理 topic.json 含 reference_framework

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 batch C 修复后存货管理主题可用于 mandatory 检查 |
| **前置条件** | P0-11 已修复 |
| **测试步骤** | Python 解析 topic.json，读取 reference_framework.mandatory，检查元素数 ≥ 4 |
| **预期结果** | mandatory 数组含入库/出库/盘点等模块定义 |
| **通过标准** | len(mandatory) >= 4，每个元素的 id/name 均非空 |
| **失败标准** | reference_framework 不存在或 mandatory 为空 |

---

### 批次 D：Schema 补齐（3 个测试用例）

#### TC-D1：current-audit.json schema 含新字段

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 project-init SKILL.md 的 schema 模板含 audit_purpose/report_type/design_observations_consumed |
| **前置条件** | P1-2/3/4 已修复 |
| **测试步骤** | grep project-init/SKILL.md 搜索这三个字段名 |
| **预期结果** | 三个字段均在 audit_state 结构中定义 |
| **通过标准** | 三处各至少 1 个匹配 |
| **失败标准** | 任一字段缺失 |

#### TC-D2：document-organizer schema 含 baseline_audit_program

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 json-schema.md 定义了 baseline_audit_program 字段 |
| **前置条件** | P0-1 已修复 |
| **测试步骤** | grep document-organizer/references/json-schema.md 搜索 "baseline_audit_program" |
| **预期结果** | 存在该字段定义 |
| **通过标准** | 至少 1 个匹配 |
| **失败标准** | 0 匹配 |

#### TC-D3：report-generator 读 .json 而非 .md

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 report-generator SKILL.md 引用 design-assessments/*.json |
| **前置条件** | P0-7 已修复 |
| **测试步骤** | grep report-generator/SKILL.md 中 design-assessments 相关行 |
| **预期结果** | 引用行含 *.json |
| **通过标准** | 存在 `design-assessments/*.json` 字样 |
| **失败标准** | 仅存在 `*.md` 引用 |

---

### 批次 E：文档统一（3 个测试用例）

#### TC-E1：CLAUDE-project.md 阶段编号一致

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 CLAUDE-project.md 与 phase_gate.py 的阶段编号统一 |
| **前置条件** | P1-11 已修复 |
| **测试步骤** | grep CLAUDE-project.md 搜索 "Phase 4" 和 "Phase 5"，确认上下文是 execution/report |
| **预期结果** | execution 对应 Phase 3，report 对应 Phase 4 |
| **通过标准** | 不存在 "Phase 4 (execution)" 或 "Phase 5 (report)" |
| **失败标准** | 仍存在旧编号 |

#### TC-E2：constitution 条款数一致

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证 constitution.md 与 CLAUDE-project.md 的约束数量声明一致 |
| **前置条件** | P1-16 已修复 |
| **测试步骤** | 1. 数 constitution.md 中"不可违反的约束"数量<br>2. grep CLAUDE-project.md 的 hard constraints 数量声明<br>3. 对比是否一致 |
| **预期结果** | 两个数字一致 |
| **通过标准** | 数字一致（例如都是 14，或都是 10 取决于选择的修复方案） |
| **失败标准** | 数字不一致 |

#### TC-E3：validate-policy-analysis.py 死代码移除

| 项目 | 内容 |
|------|------|
| **测试目标** | 验证死代码函数 check_decision_log 已移除或改为非阻断 |
| **前置条件** | P1-18 已修复 |
| **测试步骤** | 1. 读 validate-policy-analysis.py 确认 decision_log 相关代码<br>2. 或无此函数，或被明确标注为 skip |
| **预期结果** | 该检查不再对缺 decision_log 的正常输出产生 noise |
| **通过标准** | 对标准 policy-analyses JSON 不输出决策日志相关的 warn |
| **失败标准** | 仍输出 "decision_log" 相关的 false-positive warn |

---

## 四、验证步骤（执行顺序）

```
Step 1: 环境准备
  ├── 确认 Python 环境可用（3.13.12 + pandas）
  ├── 确认测试数据目录创建
  └── 确认所有修复文件已提交到 git

Step 2: 批次 A 验证（6 个 TC）
  ├── TC-A1 → TC-A2 → TC-A3 → TC-A4 → TC-A5 → TC-A6
  └── 全部通过后方可进入 B

Step 3: 批次 B 验证（5 个 TC）
  ├── TC-B1 → TC-B2 → TC-B3 → TC-B4 → TC-B5
  └── TC-B4 如超时失败但其他通过 → 记录风险，不阻断

Step 4: 批次 C 验证（3 个 TC）
  ├── TC-C1 → TC-C2 → TC-C3
  └── 全部通过

Step 5: 批次 D 验证（3 个 TC）
  ├── TC-D1 → TC-D2 → TC-D3
  └── 全部通过

Step 6: 批次 E 验证（3 个 TC）
  ├── TC-E1 → TC-E2 → TC-E3
  └── 全部通过

Step 7: 回归检查
  ├── 重跑批次 A 的关键 TC（A1, A4）
  └── 确认未引入新问题

Step 8: 输出验证报告
```

---

## 五、通过/失败标准汇总

| 批次 | TC 总数 | 最低通过数 | 阻断性 TC | 说明 |
|:---:|:---:|:---:|:---:|------|
| A | 6 | 6 | A1, A2, A4 | 全部为阻断性，必须全通过 |
| B | 5 | 4 | B1, B2, B3 | B4（超时）如失败记录风险不阻断 |
| C | 3 | 3 | C1, C3 | 全部为阻断性 |
| D | 3 | 3 | D2 | 全部必须通过 |
| E | 3 | 3 | — | 全部必须通过 |
| **总计** | **20** | **19** | — | — |

**最终判定**：
- ✅ **通过**：20/20 通过（或 B4 明确记录风险后 19/20）
- ⚠️ **条件通过**：仅 B4 失败，其余全部通过
- ❌ **不通过**：任一阻断性 TC 失败

---

## 六、风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|:---:|:---:|------|
| validate-finding.py 重写后遗漏关键校验项 | 中 | 有问题的 finding 可能通过校验 | 做新旧校验逻辑对比清单，确保不遗漏硬拦截项 |
| data_executor.py Windows 超时方案不可靠 | 中 | 死循环无法终止 | threading.Timer 失败时降级为子进程方案，作为 TC-B4 的替代路径 |
| audit_gate.py 修复后与 phase_gate 产生新冲突 | 低 | 两套闸机互斥 | 修复后在两个项目目录分别测试，确保不互相干扰 |
| constitution #10 检查脚本在无文档的测试环境中崩溃 | 中 | 测试无法执行 | 脚本对所有异常路径（目录不存在/JSON 格式错误/字段缺失）做 defensive 处理 |
| 多个修复文件产生 git 合并冲突 | 低 | 部分修复丢失 | 每批次完成后立即 commit，避免累积修改 |

---

## 七、时间安排

| 阶段 | 内容 | 预计耗时 |
|:---:|------|:---:|
| 准备 | 环境检查、测试数据准备 | 15 min |
| 批次 A 执行 | TC-A1 ~ A6 | 30 min |
| 批次 B 执行 | TC-B1 ~ B5 | 25 min |
| 批次 C 执行 | TC-C1 ~ C3 | 20 min |
| 批次 D 执行 | TC-D1 ~ D3 | 15 min |
| 批次 E 执行 | TC-E1 ~ E3 | 10 min |
| 回归检查 | 重跑 A1/A4 | 5 min |
| 报告编写 | 汇总结果 | 10 min |
| **总计** | | **~2 小时** |

---

## 八、验证报告模板

验证完成后输出以下格式：

```
## 数据流转整改验证报告

### 执行概要
- 执行时间: YYYY-MM-DD HH:MM
- 总测试用例: 20
- 通过: N, 失败: N, 跳过: N
- 判定: ✅ 通过 / ⚠️ 条件通过 / ❌ 不通过

### 详细结果

| TC | 批次 | 状态 | 耗时 | 备注 |
|:---:|:---:|:---:|:---:|------|
| A1 | A | ✅ | 2s | — |
| A2 | A | ✅ | 1s | — |
| ... | ... | ... | ... | ... |

### 失败项
| TC | 失败原因 | 根因分析 | 建议 |
|:---:|------|------|------|

### 风险记录
| 风险 | 状态 | 说明 |
|------|:---:|------|
```

---

*本验证计划在整改代码完成后立即执行。建议先完成批次 A+B 的修复和验证（最关键），再推进 C/D/E。*
