# 内部审计 AI 助手 — 操作手册

> 版本：2026-07-10-2 | 阅读时间：6 分钟

---

## 这是什么东西

这个系统帮你从制度分析一路跑到审计报告。你负责四件事：**放文件、做判断、给数据、确认输出**。系统负责记住流程、检查质量、防止遗漏。

**怎么和系统沟通**：你用日常中文说就行——"列出高风险发现"、"搜一下提到废料的发现"、"对比一下今年和去年的存货审计"——系统自动理解你的意思，运行对应工具，把结果翻译成人话汇报给你。不需要记命令、不需要学技术。本手册末尾附了命令速查表，但绝大多数时候你不需要看。

把它想象成一个不会忘事的审计实习生——你告诉它做什么审计、把公司制度给它、把证据给它，它会帮你分析、出程序、写发现、生成报告。但最终判断永远是你做。

---

## 怎么开始一个新审计项目

**第一步：建一个文件夹**

在你习惯放文件的地方（比如 D 盘），新建一个文件夹。名字随便取，自己能记住就行，比如"2026年7月存货审计"。

**第二步：在这个文件夹里打开 PowerShell**

打开你刚建的文件夹，然后：

> 在文件夹顶部地址栏（显示路径的那个白条）里，直接输入 `powershell`，然后按回车。

（备用方法：按住 Shift 键不放，在文件夹空白处点右键，菜单里会多出一项"在此处打开 PowerShell 窗口"，点它。）

这时候会弹出一个蓝底白字的窗口——这就是 PowerShell，别怕，只需要在里面粘贴一行命令。

**第三步：粘贴部署命令**

把下面这行命令完整复制，在 PowerShell 窗口里点右键（或按 Ctrl+V），粘贴进去，然后按回车：

```
powershell -File "D:\Nut\00_my_digital\12_AGI\skills\internal-audit\setup-project.ps1" -ProjectDir .
```

> ⚠️ **注意**：上面这行命令中间那串路径 `D:\Nut\00_my_digital\12_AGI\skills\internal-audit\setup-project.ps1` 是系统工具的存放位置。如果你的工具放在别的盘或别的文件夹，把路径改成实际的就行。不知道怎么改的话，问一下帮你装系统的人。

跑完后，窗口里会出现一堆文字，最后一行如果是 `Done` 开头，就说明部署成功了。

如果你希望这个项目**不受后续系统更新的影响**（正式审计项目建议这么做），用下面这行代替：

```
powershell -File "D:\Nut\00_my_digital\12_AGI\skills\internal-audit\setup-project.ps1" -ProjectDir . --stable
```

**第四步：启动 AI 助手**

在同一个 PowerShell 窗口里，输入下面这行命令，按回车：

```
claude
```

等几秒钟，AI 启动后，对它说"开始新审计项目"就行了。

---

**以后每次新审计**：重复第一步和第二步（换一个新文件夹），其他步骤一样。

**总结一下你只需要学会三个操作**：① 建文件夹 ② 在文件夹地址栏输入 powershell ③ 粘贴一行命令。三次搞定。

---

## 每一步做什么

审计分 6 个阶段。系统会自动检查你做完了没有，做完了才能进入下一步。

| 阶段 | 你要做什么 | 系统会给你什么 | 怎么算做完了 |
|------|-----------|---------------|-------------|
| **Phase 0 初始化** | 告诉 AI 你要审计什么主题 | 帮你搭好文件夹结构、选好审计主题 | 审计主题确定了 |
| **Phase 1 制度分析** | 把公司制度文件（Word/PDF/扫描件）放进来 | 控制点清单 + 风险点清单 + 设计缺陷观察 | 每份制度都有分析结果文件 |
| **Phase 1.5 访谈** | 告诉 AI 你想访谈谁、把访谈记录放进来 | 访谈提纲 + 线索追踪 + 对审计程序的补充建议 | 访谈线索已纳入审计程序 |
| **Phase 2-3 程序+执行** | 按审计程序去现场收集证据 | 审计程序清单 + 帮你分析你拿回来的数据 | 每个审计步骤都有结果 |
| **Phase 4 发现** | 把证据给 AI，告诉它你发现了什么 | 结构化审计发现 + 风险定级 + 根因分析 | 至少 1 个 finding 文件生成 |
| **Phase 5 报告** | 选择报告类型 | 正式审计报告 | 报告文件生成 |

**Phase 0-1 是"在办公室看制度"，Phase 2-3 是"去现场查"，Phase 4-5 是"回办公室写报告"。**

---

## 常见情况怎么处理

### 跑一半发现制度文件少放了

对 AI 说："回退到 Phase 1，我补了一份制度文件"。系统会回到 Phase 1，补分析完，再往前走。

### 被审计方不配合、不给证据

对 AI 说清楚"哪个环节对方不给"。系统会标记为"证据缺失"，并分析三种可能原因：这业务根本没发生 / 管理有漏洞 / 证据被销毁了。

### 访谈后发现了新线索

系统会自动检测"有未使用的新线索"，提示你更新审计程序。你只需要对 AI 说"更新审计程序"，系统会增量补充，不推翻之前的内容。

### 证据等级不够支撑高风险结论

系统会自动拦截——高风险发现必须有 A 级（系统导出的数据）或 E 级（第三方机构出的证明）证据。被拦截时，你要么补更硬的证据，要么降级为中风险。

### 想查当前项目的发现

你对 AI 说就行（"列出高风险发现" / "搜一下提到废料的发现" / "汇总一下当前情况"），AI 自动跑对应命令。

### 系统阶段卡住了、前进不了

对 AI 说"看看当前进度"或"能不能进入下一阶段"。AI 会跑闸机检查告诉你缺什么。补齐条件后说"前进"。

### 想查之前某个审计项目的数据（跨项目查询）

**先说怎么用**：对 AI 说就行——"把以前所有高风险发现列出来"、"搜一下所有项目里提到废料的发现"、"对比一下 2025 和 2026 年的存货审计"。

**第一次使用前**：需要先把之前的项目登记一下。对 AI 说"帮我把 D 盘审计项目文件夹下 2025 年的废料审计项目注册进来"，AI 帮你登记。登记完以后查询就自动跨项目了。

---

## 升级系统

系统不定期更新。如果你用的是 `--stable` 模式部署的项目，需要手动升级：

```
powershell -File D:\Nut\...\update-project.ps1 -ProjectDir .
```

脚本会告诉你更新了什么，你确认后才升级。旧版本自动备份到 `.backup/` 文件夹，出问题可以退回去。

如果你用的是默认模式（junction 链接），不需要手动升级——改完自动生效。但这也意味着系统行为可能在你审计过程中变化。正式项目建议用 `--stable`。

---

## 出问题了怎么办

### 常见报错

| 报错信息 | 什么意思 | 怎么办 |
|---------|---------|--------|
| `policy-analyses/ 无 JSON` | 没做制度分析 | 先跑 Phase 1 |
| `退出条件未满足` | 当前阶段还有没做完的 | 看提示里缺什么，补齐 |
| `tool-check 不可用` | 当前阶段不能用这个工具 | 先用当前阶段允许的工具 |
| `validate --strict 阻断` | 输出的文件格式不对 | 看阻断提示，让 AI 修正 |
| `找不到 current-audit.json` | 不在审计项目文件夹里 | 确认你 cd 到了对的文件夹 |

### 想回退到上一个阶段

对 AI 说"回退到 Phase X"。系统会保存当前状态快照（最近 20 个），不会丢数据。

### 系统行为突然变了（junction 模式的审计项目）

说明黄金源被更新了。如果你需要行为稳定，重新部署一个新项目，用 `--stable` 模式。

### 升级后系统行为不对

去 `.backup/` 文件夹找到升级前的文件，手动覆盖回来。然后告诉我们你遇到的问题。

---

## 一次完整审计的全流程（例子）

```
第一天（在办公室）：
  AI："你想审计什么？"  → 你："废料处置" 
  AI："把制度文件放进来"  → 你拖进去 5 份 Word
  AI：输出 5 份分析结果 + 12 个控制缺口

第二天（在办公室）：
  AI："你想访谈谁？"  → 你："废料仓管、财务出纳"
  AI：生成访谈提纲
  你去访谈，把记录拿回来
  AI：输出 3 条新线索，更新审计程序

第三天（在现场）：
  AI 生成完整审计程序（10 章）
  你按程序去废料仓盘点、去财务部调凭证

第四-五天（回办公室）：
  你：把盘点差异表、银行流水给 AI
  AI：生成 5 个 finding（2 高 2 中 1 低）
  AI：出正式审计报告
  AI："报告已生成，请审核"
```

---

## 底线规则

这 10 条规则是硬底线，系统会自动检查，你不需要刻意记：

1. 没证据不能出 finding
2. 涉嫌舞弊的，不论金额大小，都标高风险
3. 高风险必须有硬证据（系统数据或第三方证明）
4. 每份证据要标可靠等级（A/B/C/D/E）
5. 每个 finding 要写清楚证据是谁、什么时候、从哪来的
6. 只能说"疑似舞弊"，不能说"构成舞弊"（那是法院说的）
7. 不能用示例数据，必须用真实公司数据
8. 系统有工具就用工具，别自己造轮子
9. 证据缺失本身就是一条线索——分析为什么缺
10. 制度分析要对照着制度清单一个一个看，不能漏

---

## 相关文件一览

| 文件 | 给谁看的 | 里面有什么 |
|------|---------|-----------|
| `OPS.md`（本文件） | 你（审计师） | 操作说明 |
| `CLAUDE.md` | AI | AI 的行为指令 |
| `constitution.md` | AI | 硬约束规则 |
| `_shared/scripts/` | AI 调用 | 闸机检查、质量校验、数据查询 |
| `audit-topics/about-me.md` | 你写、AI 读 | 被审计公司背景 |
| `audit-topics/my-config.md` | 你写、AI 读 | 系统配置 |
| `internal-audit-workspace/` | 你和 AI 共用 | 所有审计工作产物 |
| `memory/` | AI 记 | 项目进展、决策记录 |

---

## 命令速查表

大多数时候你对 AI 说人话就行，不需要看这个。这表是给想直接敲终端的人用的。

### 闸机控制

```
python _shared/scripts/phase_gate.py status      查当前阶段
python _shared/scripts/phase_gate.py check       能否进下一阶段
python _shared/scripts/phase_gate.py advance     进入下一阶段
python _shared/scripts/phase_gate.py rollback --to <阶段> --reason "<原因>"   回退
```

### 数据查询

```
python _shared/scripts/queries.py findings --risk 高              高风险发现
python _shared/scripts/queries.py findings --status 待整改        待整改的发现
python _shared/scripts/queries.py search "关键词"                 全文搜索
python _shared/scripts/queries.py trace F-2026-001                追溯发现链
python _shared/scripts/queries.py summary                         汇总统计
python _shared/scripts/queries.py decide D-003                    查某个决策点
python _shared/scripts/queries.py decide --all                    列出所有决策点
```

### 跨项目查询（需先 register）

```
python _shared/scripts/queries.py register --list                                         列出已注册
python _shared/scripts/queries.py register --path <目录> --topic <主题> --period <期间>     注册项目
python _shared/scripts/queries.py register --remove <ID>                                   删除注册
python _shared/scripts/queries.py findings --risk 高 --cross-project                      跨项目查询
python _shared/scripts/queries.py search "关键词" --cross-project                          跨项目搜索
python _shared/scripts/queries.py compare --topic <主题> --from 2025 --to 2026 --cross-project  跨项目对比
```

### 系统部署

```
powershell -File setup-project.ps1 -ProjectDir .             默认模式（junction，实时同步）
powershell -File setup-project.ps1 -ProjectDir . --stable    稳定模式（锁定版本）
powershell -File update-project.ps1 -ProjectDir .            增量升级（stable 模式用）
``` |
